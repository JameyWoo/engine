<article class="baidu_pl">
		<div id="article_content" class="article_content clearfix csdn-tracking-statistics" data-pid="blog"  data-mod=popu_307  data-dsm = "post" >
								<div class="article-copyright">
                  					
					版权声明：看我干嘛? 你又没打算转载我的博客~					https://blog.csdn.net/wjh2622075127/article/details/83865071				</div>
								<link rel="stylesheet" href="https://csdnimg.cn/release/phoenix/template/css/ck_htmledit_views-f57960eb32.css" />
        								            <div id="content_views" class="markdown_views prism-atom-one-light">
							<!-- flowchart 箭头图标 勿删 -->
							<svg xmlns="http://www.w3.org/2000/svg" style="display: none;"><path stroke-linecap="round" d="M5,0 0,2.5 5,5z" id="raphael-marker-block" style="-webkit-tap-highlight-color: rgba(0, 0, 0, 0);"></path></svg>
							<p>今天我部署了几个vultr的服务器, ping了一下, 延迟还可以, 只有200ms左右. 但是用xshell进行连接时死活连不上, 找了些资料, 知道了原来ip被墙可能是整个ip被墙, 也可能是端口被墙.</p>
<p>ssh默认的端口号为22/tcp. 因此, 我们需要判断ip的此端口是否被墙.</p>
<p>被墙的情况是在国内无法访问, 而在国外可以访问, 因此需要对比国内外.</p>
<p>下面给出两个端口测试网站:</p>
<p><a href="https://tool.lu/portscan/" rel="nofollow">国内</a></p>
<p><a href="https://www.yougetsignal.com/tools/open-ports/" rel="nofollow">国外</a></p>
<p>在里面输入ip, 输入要测试的端口, 然后就可以check了.</p>
<p>如这个国内的, 测试这个ip: <code>104.207.153.197</code>, 显示端口已关闭, (此ip已销毁)</p>
<p><img src="https://img-blog.csdnimg.cn/20181108172937293.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3dqaDI2MjIwNzUxMjc=,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"></p>
<p>而国外的, 显示是open.<br>
<img src="https://img-blog.csdnimg.cn/20181108172910667.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3dqaDI2MjIwNzUxMjc=,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"></p>
<p>说明ip被墙.</p>
<hr>
<p>推荐一个ip测试网站: <a href="https://tools.ipip.net/ping.php" rel="nofollow">https://tools.ipip.net/ping.php</a></p>
<hr>
<p>常用端口号：</p>
<p>代理服务器常用以下端口：</p>
<p>（1）. HTTP协议代理服务器常用端口号：80/8080/3128/8081/9080</p>
<p>（2）. SOCKS代理协议服务器常用端口号：1080</p>
<p>（3）. FTP（文件传输）协议代理服务器常用端口号：21</p>
<p>（4）. Telnet（远程登录）协议代理服务器常用端口：23</p>
<p>HTTP服务器，默认的端口号为80/tcp（木马Executor开放此端口）；</p>
<p>HTTPS（securely transferring web pages）服务器，默认的端口号为443/tcp 443/udp；</p>
<p>Telnet（不安全的文本传送），默认端口号为23/tcp（木马Tiny Telnet Server所开放的端口）；</p>
<p>FTP，默认的端口号为21/tcp（木马Doly Trojan、Fore、Invisible FTP、WebEx、WinCrash和Blade Runner所开放的端口）；</p>
<p>TFTP（Trivial File Transfer Protocol ），默认的端口号为69/udp；</p>
<p>SSH（安全登录）、SCP（文件传输）、端口重定向，默认的端口号为22/tcp；</p>
<p>SMTP Simple Mail Transfer Protocol (E-mail)，默认的端口号为25/tcp（木马Antigen、Email Password Sender、Haebu Coceda、Shtrilitz Stealth、WinPC、WinSpy都开放这个端口）；</p>
<p>POP3 Post Office Protocol (E-mail) ，默认的端口号为110/tcp；</p>
<p>WebLogic，默认的端口号为7001；</p>
<p>WebSphere应用程序，默认的端口号为9080；</p>
<p>WebSphere管理工具，默认的端口号为9090；</p>
<p>JBOSS，默认的端口号为8080；</p>
<p>TOMCAT，默认的端口号为8080；</p>
<p>WIN2003远程登陆，默认的端口号为3389；</p>
<p>Symantec AV/Filter for MSE ,默认端口号为 8081；</p>
<p>Oracle 数据库，默认的端口号为1521；</p>
<p>ORACLE EMCTL，默认的端口号为1158；</p>
<p>Oracle XDB（ XML 数据库），默认的端口号为8080；</p>
<p>Oracle XDB FTP服务，默认的端口号为2100；</p>
<p>MS SQL*SERVER数据库server，默认的端口号为1433/tcp 1433/udp；</p>
<p>MS SQL*SERVER数据库monitor，默认的端口号为1434/tcp 1434/udp；</p>
<p>QQ，默认的端口号为1080/udp</p>

            </div>
						<link href="https://csdnimg.cn/release/phoenix/mdeditor/markdown_views-258a4616f7.css" rel="stylesheet">
                      </div>
	</article>