                                                                                                                                                         包含在头文件     #include &lt; queue &gt;   有一下常用函数     push(x) 把x压入队尾    pop() 弹出队顶元素    front() 返回队顶元素    back() 返回队尾元素    size() 返回队列的元素数    empty() 返回bool数   简单代码示例    #include &lt;iostream&gt; #include &lt;queue&gt;  using namespace std;  int main() {     queue&lt; int &gt;que;     for(int i = 0; i &lt; 10; ++i) {         que.push(i);     }     cout &lt;&lt; que.size() &lt;&lt; endl;     if(que.empty()) {         cout &lt;&lt; "it is empty\n";     } else {         cout &lt;&lt; "it is not empty\n";     }     cout &lt;&lt; que.front() &lt;&lt; endl;     que.pop();     cout &lt;&lt; que.front() &lt;&lt; endl;     cout &lt;&lt; que.back() &lt;&lt; endl;      //que.clear(); //没有clear函数       cout &lt;&lt; que.size() &lt;&lt; endl; }  queue在那些方面有应用?   我知道广度优先搜索(BFS)用到了队列  值得注意的是, 队列queue是没有clear函数的, 所以要用其他方法全部清零  方法一:   直接赋值法   queue &lt; int &gt; que; //...... que = queue &lt; int &gt;();  方法二:   遍历pop法   queue &lt; int &gt; que; //...... while(!que.empty()) {     que.pop(); }  方法三:  使用swap，这种是最高效的，定义clear，保持STL容器的标准。(摘抄)     void clear(queue&lt;int&gt;&amp; q) {     queue&lt;int&gt; empty;     swap(empty, q); }                                            