                                                                                                                                                         一、branch命令 1、git branch branch-name 新建分支branch  2、git branch -d branch-name 删除分支  3、git branch 或git status查看当前分支，git chechout branch-name 切换当前分支，可以看到使用git branch 是当前分支前面会有*符号  二、merge合并分支 1、在分支状态下无法push，使用merge命令将其合并到master中 先切换到master git checkout master 然后合并分支git merge branch-test 最后push git push 在github上查看发现其已经更新  2、合并冲突(会产生一些冲突的情况） 三、pull ，更新本地仓库，显化差异 如果在本地仓库修改了一个文件，在push到远程仓库之前，远程仓库也发生了本地不知道的更改，那么就会发生冲突，无法更改。  这是就需要用到pull命令，将远程仓库更新的内容pull到本地，查看冲突文件，发现远程和本地的冲突被标示出来。  四、clone，从远程仓库工作的流程 1、在工作区克隆远程仓库 git clone 'url'  2、对readme文件进行修改和增加一个文件  3、进行add，commit，push之后，在远程仓库看到更改。                                              